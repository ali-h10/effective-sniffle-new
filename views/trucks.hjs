<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Browse Trucks - Food Truck Ordering System</title>
    <link rel="stylesheet" href="/styles/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/style.css">
    <style>
        :root {
            --primary-color: #CA9747;
            --dark-bg: #0c0f22;
            --text-color: #fff7e6;
            --secondary-text: #cfc6bd;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--dark-bg);
            color: var(--text-color);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
        }

        /* Navigation */
        .navbar {
            background: linear-gradient(135deg, var(--dark-bg) 0%, rgba(202, 151, 71, 0.1) 100%);
            border-bottom: 2px solid var(--primary-color);
            padding: 1rem 0;
            box-shadow: 0 4px 12px rgba(202, 151, 71, 0.15);
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .nav-brand {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--primary-color);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
        }

        .nav-links a {
            color: var(--text-color);
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
            padding: 0.5rem 0;
        }

        .nav-links a:hover,
        .nav-links a.active {
            color: var(--primary-color);
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--primary-color);
            transition: width 0.3s ease;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .logout-btn {
            background: var(--primary-color);
            color: var(--dark-bg);
            padding: 0.5rem 1rem;
            border-radius: 5px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .logout-btn:hover {
            background: #d4a956;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(202, 151, 71, 0.3);
        }

        /* Header */
        .page-header {
            text-align: center;
            margin: 3rem 0 2rem;
            padding: 0 2rem;
        }

        .page-header h1 {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
            font-weight: bold;
        }

        .page-header p {
            color: var(--secondary-text);
            font-size: 1.1rem;
        }

        /* Search Bar */
        .search-container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 2rem;
        }

        .search-box {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .search-input-wrapper {
            flex: 1;
            min-width: 250px;
            position: relative;
        }

        .search-input {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 2px solid var(--primary-color);
            background: rgba(202, 151, 71, 0.05);
            color: var(--text-color);
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .search-input:focus {
            outline: none;
            background: rgba(202, 151, 71, 0.15);
            box-shadow: 0 0 12px rgba(202, 151, 71, 0.3);
        }

        .search-input::placeholder {
            color: var(--secondary-text);
        }

        .filter-btn {
            padding: 0.75rem 1.5rem;
            background: var(--primary-color);
            color: var(--dark-bg);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .filter-btn:hover {
            background: #d4a956;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(202, 151, 71, 0.3);
        }

        /* Main Container */
        .main-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem 3rem;
        }

        /* Trucks Grid */
        .trucks-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
            margin: 2rem 0;
        }

        .truck-card {
            background: linear-gradient(135deg, rgba(202, 151, 71, 0.1) 0%, rgba(202, 151, 71, 0.05) 100%);
            border: 2px solid var(--primary-color);
            border-radius: 12px;
            padding: 1.5rem;
            transition: all 0.3s ease;
            cursor: pointer;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .truck-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 24px rgba(202, 151, 71, 0.3);
            border-color: #d4a956;
        }

        .truck-icon {
            width: 80px;
            height: 80px;
            background: var(--primary-color);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            margin: 0 auto;
        }

        .truck-header {
            text-align: center;
        }

        .truck-name {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .truck-description {
            color: var(--secondary-text);
            font-size: 0.95rem;
            line-height: 1.5;
        }

        .status-badge {
            display: inline-block;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
            text-align: center;
        }

        .status-available {
            background: rgba(76, 175, 80, 0.2);
            color: #4CAF50;
            border: 1px solid #4CAF50;
        }

        .status-unavailable {
            background: rgba(244, 67, 54, 0.2);
            color: #f44336;
            border: 1px solid #f44336;
        }

        .truck-actions {
            display: flex;
            gap: 1rem;
            margin-top: auto;
        }

        .view-menu-btn {
            flex: 1;
            padding: 0.75rem 1rem;
            background: var(--primary-color);
            color: var(--dark-bg);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .view-menu-btn:hover {
            background: #d4a956;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(202, 151, 71, 0.3);
        }

        .view-menu-btn:disabled {
            background: #666;
            color: #999;
            cursor: not-allowed;
            transform: none;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
        }

        .empty-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        .empty-state h2 {
            color: var(--primary-color);
            margin-bottom: 1rem;
        }

        .empty-state p {
            color: var(--secondary-text);
            margin-bottom: 2rem;
        }

        /* Loading */
        .loading {
            text-align: center;
            padding: 3rem;
            color: var(--primary-color);
            font-size: 1.2rem;
        }

        .spinner {
            display: inline-block;
            width: 40px;
            height: 40px;
            border: 4px solid rgba(202, 151, 71, 0.3);
            border-top: 4px solid var(--primary-color);
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .nav-links {
                gap: 1rem;
            }

            .page-header h1 {
                font-size: 1.8rem;
            }

            .trucks-grid {
                grid-template-columns: 1fr;
            }

            .search-box {
                flex-direction: column;
            }

            .search-input-wrapper {
                min-width: 100%;
            }
        }

        /* Toast Notification */
        .toast {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            background: var(--primary-color);
            color: var(--dark-bg);
            padding: 1rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            z-index: 1000;
            animation: slideIn 0.3s ease;
            box-shadow: 0 4px 12px rgba(202, 151, 71, 0.3);
        }

        @keyframes slideIn {
            from {
                transform: translateX(400px);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-brand">🚚 Food Truck Ordering</a>
            <ul class="nav-links">
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/trucks" class="active">Browse Trucks</a></li>
                <li><a href="/cart">Cart</a></li>
                <li><a href="/myorders">My Orders</a></li>
                <li><a href="/logout" class="logout-btn">Logout</a></li>
            </ul>
        </div>
    </nav>

    <!-- Page Header -->
    <div class="page-header">
        <h1>Browse Trucks</h1>
        <p>Discover delicious food from amazing food trucks in your area</p>
    </div>

    <!-- Search Container -->
    <div class="search-container">
        <div class="search-box">
            <div class="search-input-wrapper">
                <input 
                    type="text" 
                    class="search-input" 
                    id="searchInput" 
                    placeholder="Search trucks by name..."
                >
            </div>
            <button class="filter-btn" id="filterBtn">Filter</button>
        </div>
    </div>

    <!-- Main Container -->
    <div class="main-container">
        <!-- Loading State -->
        <div id="loadingState" class="loading" style="display: none;">
            <div class="spinner"></div>
            <p>Loading trucks...</p>
        </div>

        <!-- Trucks Grid -->
        <div id="trucksGrid" class="trucks-grid"></div>

        <!-- Empty State -->
        <div id="emptyState" class="empty-state" style="display: none;">
            <div class="empty-icon">🚫</div>
            <h2>No Trucks Available</h2>
            <p>We couldn't find any food trucks matching your search. Try adjusting your filters or check back later.</p>
        </div>
    </div>

    <script src="/js/jquery-2.2.0.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <script src="/src/trucks.js"></script>
</body>
</html>
